<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.gatech.seclass.crypto6300">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DisableCryptogramActivity"></activity>
        <activity android:name=".SelectActionActivity" />
        <activity android:name=".CryptogramInfoActivity" />
        <activity android:name=".ViewStatisticActivity" />
        <activity android:name=".SolveGameMainActivity" />
        <activity android:name=".SolveGameBetActivity" />
        <activity android:name=".ViewPlayerListActivity" />
        <activity android:name=".CreateCryptogramActivity" />
        <activity android:name=".MainMenuActivity" />
        <activity android:name=".ExistingPlayerActivity" />
        <activity android:name=".NewPlayerActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>